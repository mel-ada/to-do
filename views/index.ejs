<!DOCTYPE html>
<html>

<head>
    <title>
        <%= title %>
    </title>
    <link rel='stylesheet' href='/stylesheets/style.css' />

</head>

<body>
    <h1><%= title %></h1>
    <p>Welcome to
        <%= title %>
    </p>
    <form method="POST" action="/api/todo" class="store">
        Enter To Do List item:
        <br>
        <input type="text" name="to-do-item">
        <br>
        <button type="add" value="add">Add</button>
    </form>
    <div class="list-body" To-Do>
        <div class="panel-body">
            <span class="list-group">
                <div>
                  <% for( let i = 0; i < todos.length; i++ ) { %>
                    <div>
                      <input type="checkbox"/>
                      <div id="<%= i %>" onclick="clicky('<%= i %>')" style="display: inline-block" contentEditable="false" action="edit" method="PUT">
                      <%= todos[ i ].task %>
                      </div>
                    </div>
                  <% } %>
              </div>
            </span>
        </div>
        <button type="complete" name="complete">Mark as Complete</button>
    </div>
    <script src="main.js"></script>
</body>

</html>
